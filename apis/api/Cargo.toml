[package]
name = "api"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
vespera = "0.1.2"
tokio = { version = "1.0", features = ["full"] }
tower-http = { version = "0.6", features = ["cors"] }

[build-dependencies]
vespera = "0.1.2"