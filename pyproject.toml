[project]
name = "devfive-template"
version = "0.1.0"
requires-python = ">=3.14,<4.0"

[tool.uv.workspace]
members = ["apis/*"]

[dependency-groups]
dev = ["ruff>=0.12.12"]

[tool.ruff.lint]
fixable = ["ALL"]
